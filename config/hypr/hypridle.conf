# Hypridle config: lock before sleep only.
# Idle-time locking is disabled because hyprlock can collapse Steam Remote Play
# framerate once the locked screen takes focus.

general {
    lock_cmd = ~/.config/hypr/scripts/steamlink-host-mode maybe-lock
    before_sleep_cmd = ~/.config/hypr/scripts/steamlink-host-mode maybe-lock
}

# User-local application defaults.
# Source this file after defaults.conf where commands consume these values.
$filemanager = dolphin
$applauncher = walker
$terminal = ghostty
$idlehandler = hypridle

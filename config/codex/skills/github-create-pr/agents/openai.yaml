interface:
  display_name: "GitHub Create PR"
  short_description: "Open PRs using draft-message text"
  default_prompt: "Use $github-create-pr to derive a PR title and body through $draft-commit-message, let me review them, then open the PR."

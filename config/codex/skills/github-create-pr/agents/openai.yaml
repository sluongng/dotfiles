interface:
  display_name: "GitHub Create PR"
  short_description: "Draft and open GitHub pull requests"
  default_prompt: "Use $github-create-pr to draft a PR title and description, ask for my review, then open the PR."
